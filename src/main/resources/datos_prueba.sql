CREATE PROCEDURE IF NOT EXISTS van_los_datos()
BEGIN
    IF NOT EXISTS(

        SELECT * FROM logs 
   )

   THEN 

   INSERT INTO usuarios (tipo_usuario,usuario,activo,clave)
   
   
   INSERT INTO clientes (nombre,rut,url,usuario) 

    INSERT INTO consultores(usuario,activo)

GO